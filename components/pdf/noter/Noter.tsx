import { PDFDocument } from "@/lib/types/pdf";
import NotesTabsManager from "./NotesTabsManager";

export default function Noter({ pdfDocument }: { pdfDocument: PDFDocument }) {
  return (
    <div className="h-full  w-full">
      <h1>Noter</h1>
    </div>
  );
}
